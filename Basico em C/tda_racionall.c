#include <stdio.h>
#include <conio.h>

int main(){
    int i , N1, D1, N2, D2;

    scanf("%d", &i);

    if (i>0)
    {
        while (i>0)
        {
            scanf("%d %c %d %c %d %c %d", );
        }
        
    }
    

    return 0;
}